{{- range list "traefik-public" "traefik-private" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: immich{{ if eq . "traefik-private" }}-private{{ end }}
  namespace: {{ $.Release.Namespace }}
spec:
  ingressClassName: {{ . }}
  rules:
    - host: immich.{{ $.Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: immich
                port:
                  number: 2283
  tls:
    - hosts:
        - immich.{{ $.Values.domain }}
      secretName: default-tls
---
{{- end }}
