{{- range list "traefik-public" "traefik-private" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plex{{ if eq . "traefik-public" }}-public{{ end }}
  {{- if eq . "traefik-public" }}
  annotations:
    external-dns.alpha.kubernetes.io/target: home.{{ $.Values.domain }}
  {{- end }}
  namespace: {{ $.Release.Namespace }}
spec:
  ingressClassName: {{ . }}
  rules:
    - host: plex.{{ $.Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plex
                port:
                  number: 32400
  tls:
    - hosts:
        - plex.{{ $.Values.domain }}
      secretName: default-tls
---
{{- end }}
