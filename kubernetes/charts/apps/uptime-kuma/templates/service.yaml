apiVersion: v1
kind: Service
metadata:
  name: uptime-kuma
  namespace: {{ .Release.Namespace }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 3001
      protocol: TCP
      name: http
  selector:
    app: uptime-kuma
