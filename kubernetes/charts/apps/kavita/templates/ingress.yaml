apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kavita
  namespace: {{ .Release.Namespace }}
spec:
  ingressClassName: traefik-public
  rules:
    - host: kavita.{{ .Values.global.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kavita
                port:
                  number: 5000
  tls:
    - hosts:
        - kavita.{{ .Values.global.domain }}
      secretName: default-tls
