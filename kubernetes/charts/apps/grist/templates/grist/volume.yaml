apiVersion: longhorn.io/v1beta2
kind: Volume
metadata:
  name: grist-persist
  namespace: longhorn-system
  labels:
    recurring-job-group.longhorn.io/backups-enabled: enabled
    recurring-job-group.longhorn.io/fstrim-enabled: enabled
spec:
  size: {{ mul .Values.persistVolumeSizeGi 1024 | mul 1024 | mul 1024 | quote }}
  dataLocality: best-effort
  numberOfReplicas: 2
  accessMode: rwo
  frontend: blockdev
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: grist-persist
spec:
  capacity:
    storage: {{ .Values.persistVolumeSizeGi }}Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: grist-persist
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grist-persist
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  volumeName: grist-persist
  resources:
    requests:
      storage: {{ .Values.persistVolumeSizeGi }}Gi
