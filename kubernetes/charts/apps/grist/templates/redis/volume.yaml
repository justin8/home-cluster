apiVersion: longhorn.io/v1beta2
kind: Volume
metadata:
  name: grist-redis-data
  namespace: longhorn-system
  labels:
    recurring-job-group.longhorn.io/backups-enabled: enabled
    recurring-job-group.longhorn.io/fstrim-enabled: enabled
spec:
  size: {{ mul .Values.redisVolumeSizeGi 1024 | mul 1024 | mul 1024 | quote }}
  dataLocality: best-effort
  numberOfReplicas: 2
  accessMode: rwo
  frontend: blockdev
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: grist-redis-data
spec:
  capacity:
    storage: {{ .Values.redisVolumeSizeGi }}Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: grist-redis-data
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grist-redis-data
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  volumeName: grist-redis-data
  resources:
    requests:
      storage: {{ .Values.redisVolumeSizeGi }}Gi
