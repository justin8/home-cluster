apiVersion: v1
kind: Service
metadata:
  name: dispatcharr
  namespace: {{ .Release.Namespace }}
  labels:
    app: dispatcharr
spec:
  selector:
    app: dispatcharr
  ports:
    - name: http
      port: 9191
      targetPort: 9191
