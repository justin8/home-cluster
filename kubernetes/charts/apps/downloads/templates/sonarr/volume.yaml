apiVersion: longhorn.io/v1beta2
kind: Volume
metadata:
  name: sonarr-config
  namespace: longhorn-system
  labels:
    recurring-job-group.longhorn.io/backups-enabled: enabled
    recurring-job-group.longhorn.io/fstrim-enabled: enabled
spec:
  size: {{ mul .Values.sonarrVolumeSizeGi 1024 | mul 1024 | mul 1024 | quote }}
  dataLocality: best-effort
  numberOfReplicas: 2
  accessMode: rwo
  frontend: blockdev
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: sonarr-config
spec:
  capacity:
    storage: {{ .Values.sonarrVolumeSizeGi }}Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: sonarr-config
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config
  namespace: {{ .Release.Namespace }}
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  volumeName: sonarr-config
  resources:
    requests:
      storage: {{ .Values.sonarrVolumeSizeGi }}Gi
