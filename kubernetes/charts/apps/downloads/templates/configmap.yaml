apiVersion: v1
kind: ConfigMap
metadata:
  name: downloads-config
  namespace: {{ .Release.Namespace }}
data:
  TZ: {{ .Values.TZ }}
  PUID: {{ .Values.PUID | quote }}
  PGID: {{ .Values.PGID | quote }}
