{{- range list "traefik-public" "traefik-private" }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: home-assistant{{ if eq . "traefik-public" }}-public{{ end }}
  {{- if eq . "traefik-public" }}
  annotations:
    external-dns.alpha.kubernetes.io/target: home.{{ $.Values.domain }}
  {{- end }}
  namespace: {{ $.Release.Namespace }}
spec:
  ingressClassName: {{ . }}
  rules:
    - host: home-assistant.{{ $.Values.domain }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: home-assistant-http
                port:
                  number: 8123
  tls:
    - hosts:
        - home-assistant.{{ $.Values.domain }}
      secretName: default-tls
---
{{- end }}
