apiVersion: pocketid.internal/v1alpha1
kind: PocketIDOIDCClient
metadata:
  name: tinyauth
  namespace: {{ .Release.Namespace }}
spec:
  isPublic: false
  callbackUrls:
    - https://tinyauth.{{ .Values.domain }}/api/oauth/callback/pocketid
