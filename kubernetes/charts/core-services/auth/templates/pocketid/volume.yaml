apiVersion: longhorn.io/v1beta2
kind: Volume
metadata:
  name: pocketid-data
  namespace: longhorn-system
  labels:
    recurring-job-group.longhorn.io/fstrim-enabled: enabled
    recurring-job-group.longhorn.io/backups-enabled: enabled
spec:
  size: {{ mul .Values.pocketidVolumeSizeGi 1024 | mul 1024 | mul 1024 | quote }}
  dataLocality: best-effort
  numberOfReplicas: 2
  accessMode: rwo
  frontend: blockdev
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pocketid-data
spec:
  capacity:
    storage: {{ .Values.pocketidVolumeSizeGi }}Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeHandle: pocketid-data
