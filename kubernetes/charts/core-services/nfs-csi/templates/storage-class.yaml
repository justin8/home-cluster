apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: nfs-csi
provisioner: nfs.csi.k8s.io
parameters:
  server: {{ .Values.storageDNSName }}
  share: "{{ .Values.appStoragePath }}/k8s"
reclaimPolicy: Retain
allowVolumeExpansion: true
mountOptions:
  - nfsver=4.1
