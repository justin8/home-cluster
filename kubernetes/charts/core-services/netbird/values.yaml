kubernetes-operator:
  ingress:
    enabled: true
    allowAutomaticPolicyCreation: true
  netbirdAPI:
    keyFromSecret:
      name: "netbird-mgmt-api-key"
      key: "NB_API_KEY"

# netbird-operator-config:
#   kubernetesAPI:
#     enabled: true
#   router:
#     enabled: true
